<%= form_for(memoa) do |f| %>
  <% if memoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(memoa.errors.count, "error") %> prohibited this memoa from being saved:</h2>

      <ul>
      <% memoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.text_field :picture %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
